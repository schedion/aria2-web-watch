# Normalize text files to LF to reduce cross-platform merge conflicts
* text=auto eol=lf

# Ensure scripts and configs stay LF-terminated
*.sh text eol=lf
*.conf text eol=lf
Dockerfile text eol=lf
*.md text eol=lf
